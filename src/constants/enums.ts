export enum UserRole {
  STUDENT = 1,
  LEAGUE_CADRE = 2,
  TEACHER = 3,
  SUPER_ADMIN = 4,
}

export enum ApprovalStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  WITHDRAWN = 'withdrawn',
}

export enum PartyFlowType {
  PARTY = 'party',
  LEAGUE = 'league',
}
